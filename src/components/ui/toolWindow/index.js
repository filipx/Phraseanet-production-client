import $ from 'jquery';

const toolWindow = (translations) => {
    const language = translations;

    const initialize = (cas, options) => {
    }

    return { initialize };
}

export default toolWindow;
