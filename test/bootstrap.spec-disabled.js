/* global expect */
/* eslint padded-blocks: 0*/
/* eslint no-unused-expressions: 0*/
/* eslint max-nested-callbacks: 0*/
import chai from 'chai';
import $ from 'jquery';
const expect = chai.expect;

import bootstrap from '../src/bootstrap';

describe('ProductionApplication bootstrap', () => {

    /*it('should call setupApplication', function(){
        const sampleConfig = {
            sample: true
        }
        sinon.spy(bootstrap(), 'setupApplication');
        bootstrap(sampleConfig);

        expect(bootstrap().setupApplication.withArgs(translation, sampleConfig).calledOnce).to.be.true
        bootstrap(sampleConfig).setupApplication.restore()
    })*/

});
